[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [".local-reference"]

[tool.ruff.format]
line-ending = "lf"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "RUF", "S", "SIM", "UP"]
ignore = ["E501", "S310"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]
